dont use e.code use e.key instead e.code gives different value for Numpad
run (npm start) the run (npm run watch) in another terminal to start program
if tossing function add under_score eg(parent component setDisplaySetting, then child component setDisplaySetting__)
